<div class="contents">
  <% @memos.each do |memo| %>
    <a class="content" href="memos/<%= memo[:id] %>"><%= memo[:title] %></a>
  <% end %>
</div>
<div class="footer">
  <button class="btn btn-add" onclick="location.href='/memos/new'">追加</button>
</div>
